//use super::super::{allocator, services};

pub fn init() {
    /* nothing to do, since the appropriate services are already installed */
    /*
    allocator::install_malloc_impl(
        services::alloc,
        services::alloc_zeroed,
        services::dealloc,
        services::realloc
    );
    */
}
